name: ChatOps Deploy Specific Tag

on:
  repository_dispatch:
    types: [deploy_specific_tag]

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}

jobs:
  deploy_specific_tag:
    name: Deploy Specific Tag
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      DEPLOY_TAG: ${{ github.event.client_payload.tag }}
      DEPLOY_ACTION: ${{ github.event.client_payload.action || 'deploy' }}
      DEPLOY_VERSION: ${{ github.event.client_payload.version || github.event.client_payload.tag || 'manual' }}
      SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
      SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Validate payload
        run: |
          if [ -z "${{ env.DEPLOY_TAG }}" ]; then
            echo "::error::client_payload.tag 값이 필요합니다."
            exit 1
          fi

      - name: Display deployment info
        run: |
          echo "=========================================="
          echo "🚀 특정 태그 배포 정보:"
          echo "=========================================="
          echo "  - Action: ${{ env.DEPLOY_ACTION }}"
          echo "  - Version: ${{ env.DEPLOY_VERSION }}"
          echo "  - Tag: ${{ env.DEPLOY_TAG }}"
          echo "  - Image: ${{ env.IMAGE_NAME }}:${{ env.DEPLOY_TAG }}"
          echo "  - Requester: ${{ github.event.client_payload.user }}"
          echo "=========================================="

      - name: Check EC2 configuration
        id: check_ec2
        run: |
          echo "=========================================="
          echo "🔍 EC2 설정 확인 중..."
          echo "=========================================="
          if [ -n "${{ secrets.EC2_HOST }}" ] && [ -n "${{ secrets.EC2_USER }}" ] && [ -n "${{ secrets.SSH_PRIVATE_KEY }}" ]; then
            echo "configured=true" >> $GITHUB_OUTPUT
            echo "✅ EC2 설정 완료 - 실제 배포를 진행합니다."
            echo "  - Host: ${{ secrets.EC2_HOST }}"
            echo "  - User: ${{ secrets.EC2_USER }}"
          else
            echo "configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ EC2 설정 없음 - Mock 배포를 실행합니다."
            echo ""
            echo "실제 배포를 위해 다음 Secrets를 설정하세요:"
            echo "  - EC2_HOST: EC2 인스턴스 퍼블릭 IP 또는 도메인"
            echo "  - EC2_USER: SSH 사용자명 (예: ubuntu, ec2-user)"
            echo "  - SSH_PRIVATE_KEY: SSH 프라이빗 키"
          fi
          echo "=========================================="

      - name: Deploy to AWS EC2
        if: env.DEPLOY_ACTION == 'deploy' && steps.check_ec2.outputs.configured == 'true'
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -e

            echo "=========================================="
            echo "🚀 EC2 특정 태그 배포 시작"
            echo "=========================================="

            echo "📥 Docker 이미지 Pull 중..."
            docker pull ${{ env.IMAGE_NAME }}:${{ env.DEPLOY_TAG }}

            echo "🛑 기존 컨테이너 중지 중..."
            docker stop demo-backend || true
            docker rm demo-backend || true

            echo "🚀 새 컨테이너 실행 중..."
            docker run -d \
              --name demo-backend \
              --restart unless-stopped \
              -p 80:8080 \
              -e PORT=8080 \
              ${{ env.IMAGE_NAME }}:${{ env.DEPLOY_TAG }}

            echo "⏳ 컨테이너 시작 대기 중..."
            sleep 5

            echo "=========================================="
            if docker ps | grep -q demo-backend; then
              echo "✅ 배포 완료!"
              docker ps | grep demo-backend
            else
              echo "❌ 컨테이너 실행 실패"
              docker logs demo-backend || true
              exit 1
            fi
            echo "=========================================="

      - name: Deploy to server (Mock)
        if: steps.check_ec2.outputs.configured != 'true'
        run: |
          echo "=========================================="
          echo "⚠️ Mock 배포 실행"
          echo "=========================================="
          echo ""
          echo "AWS EC2 정보가 설정되지 않았습니다."
          echo ""
          echo "실제 배포를 위해서는 다음 Secrets 설정이 필요합니다:"
          echo "  - EC2_HOST: EC2 인스턴스 주소"
          echo "  - EC2_USER: SSH 사용자명"
          echo "  - SSH_PRIVATE_KEY: SSH 프라이빗 키"
          echo ""
          echo "📦 배포 정보:"
          echo "  - Action: ${{ env.DEPLOY_ACTION }}"
          echo "  - Version: ${{ env.DEPLOY_VERSION }}"
          echo "  - Image: ${{ env.IMAGE_NAME }}:${{ env.DEPLOY_TAG }}"
          echo ""
          echo "✅ Mock 배포 완료"
          echo "=========================================="

      - name: Notify Slack (deploy success)
        if: ${{ success() && env.SLACK_BOT_TOKEN != '' && env.SLACK_CHANNEL_ID != '' }}
        continue-on-error: true
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: ${{ env.SLACK_CHANNEL_ID }}
          payload: |
            {
              "text": ":white_check_mark: *특정 태그 배포 완료*\n작업: `${{ env.DEPLOY_ACTION }}`\n이미지: `${{ env.IMAGE_NAME }}:${{ env.DEPLOY_TAG }}`\n워크플로우: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }

      - name: Notify Slack (deploy failure)
        if: ${{ failure() && env.SLACK_BOT_TOKEN != '' && env.SLACK_CHANNEL_ID != '' }}
        continue-on-error: true
        uses: slackapi/slack-github-action@v1.24.0
        with:
          channel-id: ${{ env.SLACK_CHANNEL_ID }}
          payload: |
            {
              "text": ":x: *특정 태그 배포 실패*\n작업: `${{ env.DEPLOY_ACTION }}`\n이미지: `${{ env.IMAGE_NAME }}:${{ env.DEPLOY_TAG }}`\n로그 확인: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
            }

